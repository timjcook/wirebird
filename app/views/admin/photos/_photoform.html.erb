<%= simple_form_for [:admin, @photo], :html => { :multipart => true } do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :image, :as => :file %>
  <%= f.button :submit %>
<% end %>